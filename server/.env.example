# DTales Tech Backend Environment Variables
# Copy this to .env for local development
# All variables marked REQUIRED must be set before starting the server

# ============================================================================
# DATABASE (REQUIRED)
# ============================================================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/dtales

# ============================================================================
# FRONTEND CORS (optional)
# ============================================================================
# Frontend URL for CORS; if not set, allows all origins (*)
# Set to your frontend URL in production
FRONTEND_URL=http://localhost:5173

# ============================================================================
# NODE ENVIRONMENT (optional)
# ============================================================================
# Set to 'production' to enable SSL/TLS and other production features
# Defaults to 'development'
NODE_ENV=development

# ============================================================================
# CLOUDINARY (REQUIRED)
# ============================================================================
# Cloudinary credentials for image and embedded media uploads
# Get these from https://cloudinary.com/console/settings/api-keys
#
# Do NOT use CLOUDINARY_URL env var - only use individual vars below
# Do NOT hardcode these in source code
# Do NOT commit .env to version control

CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# ============================================================================
# SERVER (optional)
# ============================================================================
# Port the backend listens on
# Defaults to 10000
PORT=10000

# ============================================================================
# VALIDATION
# ============================================================================
# The server will fail fast at startup if any REQUIRED variables are missing:
#   - CLOUDINARY_CLOUD_NAME
#   - CLOUDINARY_API_KEY
#   - CLOUDINARY_API_SECRET
#   - DATABASE_URL
#
# Check server logs if startup fails with "Missing required env var" error.
